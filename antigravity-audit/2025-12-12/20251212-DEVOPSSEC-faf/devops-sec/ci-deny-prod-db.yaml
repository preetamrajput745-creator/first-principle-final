apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ci-deny-prod-db
spec:
  podSelector:
    matchLabels:
      role: ci-runner
  egress:
  - to:
    - ipBlock:
        cidr: 10.0.0.0/8
        except:
        - 10.10.50.0/24 # Prod DB Subnet