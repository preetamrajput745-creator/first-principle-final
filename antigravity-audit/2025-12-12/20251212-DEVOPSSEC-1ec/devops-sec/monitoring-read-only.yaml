apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: monitoring-read-only
spec:
  podSelector:
    matchLabels:
      role: monitoring
  ingress:
  - from:
    - podSelector: {} # Allow scraping
  egress:
  - to: [] # Strict egress control